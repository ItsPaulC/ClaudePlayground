{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "MongoDbSettings": {
    "ConnectionString": "mongodb://localhost:27017",
    "DatabaseName": "ClaudePlayground"
  },
  "RedisSettings": {
    "ConnectionString": "localhost:6379",
    "InstanceName": "ClaudePlayground:"
  },
  "JwtSettings": {
    "Issuer": "ClaudePlayground",
    "Audience": "ClaudePlaygroundUsers",
    "ExpirationMinutes": 60,
    "RefreshTokenExpirationDays": 7
  },
  "AllowedOrigins": "",
  "IMPORTANT_JWT": "Set JWT_SECRET_KEY environment variable in production. Must be 64+ characters. Generate with: openssl rand -base64 64",
  "IMPORTANT_CORS": "Set AllowedOrigins in production to comma-separated list of allowed frontend URLs (e.g., 'https://app.example.com,https://www.example.com'). Leave empty to deny all CORS requests."
}
